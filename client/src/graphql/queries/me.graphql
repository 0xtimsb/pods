query Me {
  me {
    id
    username
  }
}

query Pod($id: Int!) {
  pod(id: $id) {
    id
    name
    stories {
      id
      title
      rank
      tasks {
        id
        title
        rank
      }
    }
  }
}
