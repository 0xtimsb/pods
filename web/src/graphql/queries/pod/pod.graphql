query Pod($id: Int!) {
  pod(id: $id) {
    id
    name
    joined
    stories {
      id
      title
      tasks {
        id
        title
      }
    }
    admins {
      id
      username
    }
    members {
      id
      username
    }
  }
}
