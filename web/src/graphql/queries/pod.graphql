query Pod($id: Int!) {
  pod(id: $id) {
    id
    name
    joined
    createdAt
    admins {
      id
      username
    }
    members {
      id
      username
    }
    stories {
      id
      title
      tasks {
        id
        title
      }
    }
  }
}
