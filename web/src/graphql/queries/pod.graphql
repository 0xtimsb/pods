query Pod($id: Int!) {
  pod(id: $id) {
    ...Pod
    stories {
      id
      title
      tasks {
        id
        title
      }
    }
    admins {
      id
      username
    }
    members {
      id
      username
    }
  }
}
