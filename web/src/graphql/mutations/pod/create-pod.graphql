mutation CreatePod($name: String!, $description: String) {
  createPod(name: $name, description: $description) {
    pod {
      id
      name
    }
    errors {
      field
      message
    }
  }
}
